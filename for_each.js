// forEach メソッド (JavaScriptの例)
// JavaScriptでも高階関数がよく使われます。例えば、forEach メソッドは配列の各要素に対して指定された関数を実行します。

// コンソールに二倍した結果を表示
const numbers = [1, 2, 3, 4, 5];
numbers.forEach(num => {
    console.log(num * 2);
});
// 出力: 2, 4, 6, 8, 10
